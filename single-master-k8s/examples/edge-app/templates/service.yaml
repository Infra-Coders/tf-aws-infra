apiVersion: v1
kind: Service
metadata:
  name: {{ include "edge-app.fullname" . }}
spec:
  type: ClusterIP
  selector:
    app: {{ include "edge-app.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
