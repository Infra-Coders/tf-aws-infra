apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "passthrough-app.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "passthrough-app.fullname" . }}
  template:
    metadata:
      labels:
        app: {{ include "passthrough-app.fullname" . }}
    spec:
      containers:
        - name: app
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}
